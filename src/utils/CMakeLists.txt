AUX_SOURCE_DIRECTORY(. UTIL_SRC_LISTS)
ADD_LIBRARY(scalpelutils SHARED ${UTIL_SRC_LISTS})

TARGET_LINK_LIBRARIES(scalpelutils hiredis)

SET_TARGET_PROPERTIES(scalpelutils PROPERTIES CLEAN_DIRECT_OUTPUT 1)

SET_TARGET_PROPERTIES(scalpelutils PROPERTIES OUTPUT_NAME "scalpelutils")
